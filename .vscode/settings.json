{
    "sonarlint.connectedMode.project": {
        // should be same connectionId you define in the User settings 
        "connectionId": "sonar",
        "projectKey": "oneai-repotemplate-python_basic"
        }
}